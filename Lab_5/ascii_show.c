#include <stdio.h>

int main() {
    char letter;

    printf("Enter a single character: ");
    scanf(" %c", &letter);

    printf("The ASCII value associated is %d\n", letter);

    return 0;
}
