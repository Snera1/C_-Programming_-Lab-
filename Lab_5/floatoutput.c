#include<stdio.h>

int main() {
	
	
	//answer will be till 6 decimal places
	//we will get the answer 2.50000.
	//the answer will be same even if we switch the data types of a and b.
	
	int a = 5;
	float b = 2;
	printf("%f" , a/b);
	
	
	return 0;
}